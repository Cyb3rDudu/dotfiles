# Bash login shell configuration
# Sources .bashrc for all login shells
# Interactive checks are handled in individual .bashrc.d/ files

# Source .bashrc if it exists
if [[ -f "$HOME/.bashrc" ]]; then
    source "$HOME/.bashrc"
fi
