# Common environment variables for all operating systems
# Loaded first (00-) to ensure base environment is set

# Editor preferences
$env.EDITOR = "vim"
$env.VISUAL = "code"
$env.PAGER = "less"

# Locale
$env.LANG = "en_US.UTF-8"
$env.LC_ALL = "en_US.UTF-8"
